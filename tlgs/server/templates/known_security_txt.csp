<%view header %>

This is a list of security.txt (RFC 9116) files known to TLGS. These files provide contact information for reporting security issues. The list includes files accessible to the TLGS crawler.

<%c++
auto security_txt = @@.get<std::shared_ptr<std::vector<std::string>>>("security_txt");
for(const auto& url : *security_txt) {
    $$ << "=> " << url << "\n";
}
%>
